<h2>Follow Requests</h2>

<% if @pending_follows.any? %>
    <% @pending_follows.each do |pending| %>
        <%= render "follow_request", follow_request: pending %>
    <% end %>
<% else %>
    <p>No pending follow requests.</p>
<% end %>